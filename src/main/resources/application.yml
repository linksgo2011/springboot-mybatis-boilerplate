server:
  port: 8080

logging.file: logs/application.log
logging:
  level:
    org:
      springframework:
        web: DEBUG
spring:
  datasource:
    url:  jdbc:h2:mem:springboot_mybatis_boilerplate;
  h2:
    console:
      enabled: true
